# Producer recap optional Python deps (same python3 as run-producer-recap.py)
# Narration docs: PERSONAL_VOICE_NARRATION.md, VOICE_HELPERS.md
opencv-python-headless>=4.8.0
numpy>=1.24.0
edge-tts>=6.1.0
Pillow>=10.0.0
# Music MVP — pitch detect + audio I/O
librosa>=0.10.0
soundfile>=0.12.0
# Optional pop/trap FX (falls back to ffmpeg loudnorm chain if missing)
# pedalboard>=0.9.0
# Word queue concat + ladder: scipy (install if missing)
# scipy>=1.10
