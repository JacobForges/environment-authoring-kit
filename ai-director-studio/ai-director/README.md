# AI Director (UI)

React studio for AI Director Studio. See the [root README](../README.md) for install and launch.

Dev: `npm run dev` → http://127.0.0.1:5174 (run `bash ../start.sh` for API on :8767).
