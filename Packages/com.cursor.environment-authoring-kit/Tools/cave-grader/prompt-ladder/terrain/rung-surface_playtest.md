## Rung: surface_playtest

Editor surface integration checks (`SurfacePlaytestValidator`).

- `GeneratedSurfaceWorld` present; terrain on Ground.
- No floating water props; vegetation count reasonable.
- No stacked flat slabs at cave mouth; entrance descent mesh present.
