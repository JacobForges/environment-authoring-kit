# Pre-build rung: prefab_catalog

**Goal:** `LavaTubePrefabCatalog` is valid for full cave builds (skipped for layout prototype).

## Fix

- [ ] Add lava-tube / cave prefabs per kit README under `Assets/`.
- [ ] Ensure catalog load path in `LavaTubePrefabCatalog` resolves in Hub.
- [ ] Do not run full cave pipeline — readiness only.
