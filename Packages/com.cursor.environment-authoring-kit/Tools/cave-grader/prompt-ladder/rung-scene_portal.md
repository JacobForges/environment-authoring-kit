# Pre-build rung: scene_portal

**Goal:** Cave portal assigned or auto-detectable (`PortalFive` / `MainScene_CavePortal`).

## Fix

- [ ] Unity: **Window → Environment Kit → Assign Cave Portal For Build (Selected)**.
- [ ] Or ensure a portal object exists in the active scene.
- [ ] `CaveBuildPortalSettings` — kit C# only if detection is broken.

Low severity — score may pass with warning only.
