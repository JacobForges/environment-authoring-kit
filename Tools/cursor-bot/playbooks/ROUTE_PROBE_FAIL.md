# ROUTE_PROBE_FAIL

1. Open `CaveBuildQualityReport.json` — confirm `mouth_unreachable` or route probe stage fail.
2. Surface: ensure trail connector from spawn sector to cave mouth (`CaveBuildSurfacePipeline`).
3. Cave: run **Fix Cave Playability** / compact route density if block tunnel sparse.
4. Re-grade; route probe steps must be > 0 and traversable.

Tag: `// [bot:rung:route_probe:DATE]`
