# COMPETITION_SMOKE_FAIL

1. Run `./Tools/competition-models/run-hub-smoke.sh` (Unity closed).
2. Ensure ONNX models in StreamingAssets + imported ModelAssets.
3. Fix only `Assets/Scripts/Competition/` — not world pipeline.

Tag: `// [bot:competition:smoke:DATE]`
