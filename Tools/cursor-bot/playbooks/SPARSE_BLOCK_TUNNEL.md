# SPARSE_BLOCK_TUNNEL

1. Confirm `NeedsCompactRouteDensityRepair(caveRoot)`.
2. Load layout from `CaveBuildMetadata` or regenerate from seed.
3. `CaveCompactRouteUtility.RebuildCompactBlockRingsOnly`.
4. Re-grade `block_tunnel` and `organic_mesh`.

Tag: `// [bot:rung:organic_mesh:DATE]`
