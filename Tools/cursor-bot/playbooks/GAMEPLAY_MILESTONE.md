# GAMEPLAY_MILESTONE

1. Mission phase = **gameplay** — read `Assets/Scripts/HubGameProgress.json`.
2. Use **hub-gameplay-completion** skill — one G1–G8 milestone per session.
3. Wire gameplay in `Assets/Scripts/` and MainScene — not package editor blockout.
4. Exit when `demoReady: true` per `docs/GAMEPLAY_DEMO_ACCEPTANCE.md`.

Tag: `// [bot:rung:gameplay:DATE]`
