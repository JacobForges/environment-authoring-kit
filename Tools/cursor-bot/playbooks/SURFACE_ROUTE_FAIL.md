# SURFACE_ROUTE_FAIL

1. Read `CaveBuildSurfaceRouteProbe.json` — issues list + `reachedCaveMouth`.
2. Fix surface trails / mouth alignment only (`CaveBuildSurfaceRouteProbe` stage).
3. Do **not** trigger Full AAA Rebuild for surface-only failures.

Tag: `// [bot:rung:surface_route:DATE]`
