# DTA Training Companion — local secrets (never commit)
# NOT the Cursor API — players bring their own coach keys.

# Optional: your Gemini key for cloud coaching (or set in Settings UI → saved locally)
GEMINI_API_KEY=""

# Optional: Cursor API key for primary coach in Auto mode
CURSOR_API_KEY=""

# Dev server only
APP_URL="http://localhost:3000"
